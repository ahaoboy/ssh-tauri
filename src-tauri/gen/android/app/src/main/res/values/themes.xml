<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ssh_tauri" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Insets are handled programmatically in MainActivity via enableEdgeToEdge(). -->
    </style>
</resources>
